import normalizeMonth from "./util/normalizeMonth";

export default class Month {
  #month: number = 0;
  #days: any[] = [];

  constructor(init: string | number | Date, year: string | number | Date) {
    if (typeof init === "number") {
    }
  }
}
